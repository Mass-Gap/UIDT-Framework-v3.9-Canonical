# Master Roadmap for L1, L4, L5 Limitations

> **Date:** 2026-05-20 cleanup revision  
> **Status:** Active research roadmap, no canonical evidence upgrade  
> **DOI:** `10.5281/zenodo.17835200`  
> **Scope:** L1, L4, L5 only. This document is a planning artifact, not a `LEDGER/CLAIMS.json` update.

---

## 0. Current Stack State

The active Phase-8 research line is staged as a PR stack:

| PR | Role | Gate Status |
|---|---|---|
| PR #459 | Session-2 Phase-8 ledger sync corrections | `TECHNICAL PASS / GUARDIAN REQUIRED` |
| PR #460 | Phase-8 delta-gamma and SU(4) audit | `TECHNICAL PASS / STACKED DEPENDENCY REQUIRED` |
| PR #461 | L1/L4/L5 reconciliation audit | `RECONCILIATION PASS / CLEANUP REQUIRED` |
| Current cleanup branch | Status/index/roadmap/errata/symbol cleanup | docs-only, no evidence promotion |

No merge, evidence-category promotion, or direct `LEDGER/CLAIMS.json` mutation is authorized by this roadmap.

---

## 1. Canonical Baseline

| Quantity | Value | Evidence | Status |
|---|---:|---|---|
| `Delta*` | `1.710 ± 0.015 GeV` | [A] | Spectral gap, not particle mass. |
| `gamma` | `16.339` | [A-] | Calibrated; not RG-derived. |
| `kappa` | `0.500 ± 0.008` | [A] | Non-minimal coupling. |
| `lambda_S` | `5*kappa^2/3 = 5/12` | [A] | RG fixed-point relation. |
| `v` | `47.7 MeV` | [A] | Canonical VEV. |
| `E_T` | `2.44 MeV` | [C] | Torsion energy. |
| `m_S` | `1.705 ± 0.015 GeV` | [D] | Prediction. |

The exact RG check remains:

```text
5*kappa^2 - 3*lambda_S = 0
```

with residual `< 1e-14` [A].

---

## 2. Evidence Discipline

- `gamma = 16.339` remains [A-].
- `gamma_bare = 49/3` is [D] as a physical UIDT identification, even though the arithmetic at `N_c=3` is exact.
- `Delta_gamma_required = 17/3000` is [D] and remains uncomputed from first-principles QFT/FRG.
- `gamma_pred = 16.338962439648224...` is [D], partial numerical hit only.
- Cosmology is capped at [C].
- SU(4) `N` remains `[TENSION ALERT]` until the convention is resolved.

---

## 3. Symbol Discipline

The legacy symbol `k_crit` is ambiguous and must be avoided unless its context is explicit.

| Symbol | Definition | Evidence | Use |
|---|---:|---|---|
| `k_T` | `4*pi*E_T = 30.6619442990... MeV` for `E_T = 2.44 MeV` | [D] chain using [C] input | S4-P1 torsion threshold. |
| `k_gamma` | approximately `Delta*/gamma = 104.66 MeV` | [D] inverse construction | D2 gamma-emergent threshold. |
| `k_crit` | legacy ambiguous label | deprecated | Use only with explicit qualifier. |

The historical value `30.707 MeV` is not exact for `E_T = 2.44 MeV`; it implies an effective `E_T ≈ 2.443585 MeV`.

---

## 4. Limitation Status

| Limitation | Current State | Evidence | Required Action |
|---|---|---|---|
| L1 | `10^10` geometric factor remains first-principles open. | [D]/[E] | Define UV/IR scales, topology/holography candidates, and falsification tests. |
| L4 | `gamma = 16.339` is not RG-derived. | [A-] value; [D] derivation attempts | Compute explicit `Pi_S(p^2)` at `p=Delta*`; then regulator-independent FRG. |
| L5 | `N=99` step count remains unjustified. | [D] | Resolve `N` convention before SU(4) promotion attempts. |

---

## 5. Phase-8 Working Results

### 5.1 Bare-Gamma Conjecture

```text
gamma_bare(N_c) = (2*N_c + 1)^2 / N_c
gamma_bare(3)  = 49/3 = 16.333333...
```

Status: [D], Stratum III. This is not an [A] derivation of `gamma = 16.339`.

### 5.2 Required Correction

```text
Delta_gamma_required = gamma - 49/3 = 17/3000 = 0.005666666...
```

Status: [D]. Positive correction required; no first-principles derivation yet.

### 5.3 Corrected S4-P1 Chain

```text
k_T          = 4*pi*E_T = 30.6619442990... MeV
v_S4P1       = sqrt(12/5)*k_T = 47.5012798530... MeV
Delta_gamma_NP = 0.00562910631489145...
gamma_pred   = 16.338962439648224...
```

Residual to `gamma = 16.339`:

```text
|gamma_pred - gamma| = 3.7560e-5
```

Status: partial numerical hit [D], not [A].

### 5.4 SU(4) Cross-Check

```text
gamma_bare(4) = 81/4 = 20.25
```

Tension:

```text
N_SU4 fixed-b0 convention = 16*11 = 176
N_SU4 pure-YM convention  = 16*(44/3) = 704/3
relative difference       = 1/3
```

Status: `[TENSION ALERT]`. SU(4) cannot be used for evidence promotion until this is resolved.

---

## 6. Active Work Order

| Priority | Task | Reason | Output |
|---:|---|---|---|
| 0 | Complete documentation cleanup. | Current state disorder can cause false derivation claims. | Cleanup PR. |
| 1 | Guardian review for PR #459. | `CANONICAL/` and `LEDGER/` are touched. | PASS/BLOCK gate. |
| 2 | Keep PR #460 stacked on #459. | Phase-8 audit depends on corrected constants. | No out-of-order merge. |
| 3 | Reconcile `STATUS.md` and `docs/research/INDEX.md`. | Remove evidence inflation and missing links. | Current cleanup branch. |
| 4 | Rename `k_crit` usages into `k_T` and `k_gamma`. | Prevent symbol collision. | Errata and updated roadmap. |
| 5 | Compute explicit scalar self-energy `Pi_S(p^2)`. | Only direct path to P1 correction. | Verification script under `verification/scripts/`. |
| 6 | Resolve SU(4) `N` convention. | Blocks SU(4) falsification test. | Short theory note. |
| 7 | Extract SU(4) lattice data from primary papers. | Needed for [B] comparison. | Data table with DOI/arXiv. |
| 8 | Run regulator-independence check for S4-P1. | Tests robustness of [D] partial hit. | Multi-regulator script. |
| 9 | Full BMW/Dyson FRG. | Only meaningful after cleanup and symbol disambiguation. | Long-running solver / no-go or partial result. |

---

## 7. Reproduction Commands

```bash
python verification/scripts/verify_session2_phase8_sync.py
python verification/scripts/verify_phase8_delta_gamma_su4_audit.py
python verification/scripts/research/verify_frg_step5_lambda3_flow.py
```

Expected classification:

```text
Phase-8 sync: PASS with residual gates and real mpmath context.
Phase-8 SU(4): PASS with partial [D] result and SU(4) tension.
FRG Step 5: NO-GO confirmed.
```

---

## 8. Falsification Criteria

| Path | Falsifier |
|---|---|
| L1 `gamma_bare=49/3` | First-principles correction is negative or greater than `0.012`. |
| S4-P1 | Regulator-independent flow does not reproduce the correction scale. |
| SU(4) scaling | SU(4) lattice comparison contradicts the `(2*N_c+1)^2/N_c` scaling. |
| L5 `N=99` | No consistent definition survives SU(N) generalization. |

---

## 9. Acceptance Status

`RESEARCH ACTIVE / NO CANONICAL PROMOTION`

The roadmap authorizes further research work only after cleanup and review gates. It does not declare L1, L4, or L5 solved.
