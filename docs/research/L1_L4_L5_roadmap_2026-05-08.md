# UIDT Framework v3.9 — Master Research Roadmap (L1, L4, L5)
**Date:** 2026-05-08  
**Status:** Audit-Corrected Master Document  

> [!WARNING]
> This document supersedes `L1_L4_L5_next_steps_2026-04-29.md`, which contained critical classification errors regarding L1 and L4.

## 1. Corrected Limitations Matrix

The previous roadmap erroneously conflated L1 (the geometric factor) with L4 (the $\gamma$ derivation). The definitive classification is:

| Limitation | Core Problem | Current Evidence | Required Next Step |
|---|---|---|---|
| **L1** | **$10^{10}$ Geometric Factor:** The gap between theoretical coupling and phenomenological lattice coupling. | [D] | Evaluate topological volume extensivity vs. bulk-boundary scaling (Path C: $N=99$ linkage). |
| **L4** | **Anomalous Dimension Derivation:** The $\gamma_{\text{ledger}} = 16.339$ value lacks a full first-principles RG derivation. | [D] | BMW-Truncation FRG to prove 1-loop vacuum correction yields $\delta\gamma_{\text{bare}} = 0.00567$. |
| **L5** | **$N=99$ RG Steps:** The exact cutoff count is empirical. | [D] | Explicit mathematical linkage of $\Sigma_T = f(E_T)$ to the cascade sequence. |

## 2. Disambiguation of $\delta\gamma$

Two distinct adjustments to $\gamma$ exist. They must never be conflated:

1.  **Finite-Size Scaling Dressing:** $\delta\gamma = 0.0047$ [B]
    *   Bridges $V \to \infty$ continuum limits to actual phenomenological fits.
2.  **Color Algebra Gap:** $\delta\gamma_{\text{bare}} = 0.00567$ [D]
    *   Bridges pure algebraic $SU(3)$ scaling ($\gamma_{\text{bare}} = 49/3$) to the canonical value ($16.339$).

## 3. L4: FRG $\gamma$ Derivation Status (Updated)

The attempt to derive $\gamma$ from first-principles RG flow is the most complex active research vector.

| Path | Approach | Status |
|---|---|---|
| **A** | Direct numerical continuum limit | ❌ **Failed:** Diverges in IR |
| **B** | Pure scaling analysis (49/3 base) + 1-loop | 🔬 **Active:** Needs BMW Truncation |
| **C** | Perturbative loop expansion | ❌ **Failed:** Breaks down near $\Delta^*$ |
| **D1**| FRG with LPA' truncation (NLO) | ❌ **Failed:** $\lambda_3$ insufficient (Factor $2857\times$ off) [NO-GO-STEP5] |
| **D2**| FRG with BMW momentum truncation | 🔬 **Active:** Requires massive computational resources |

## 4. Priority Matrix

1.  **Priority 1 (L4):** Implement BMW-Truncation for the FRG flow. The LPA' approach has definitively failed ([NO-GO-STEP5]). A momentum-dependent vertex is required to compute $\Delta\gamma_{\text{1-loop}}$.
2.  **Priority 2 (L1):** Initiate formal dimensional analysis of the $10^{10}$ gap. Evaluate $e^{N/10}$ linkages.
3.  **Priority 3 (L5):** Formalize the $N=99$ sequence algebraically.

## 5. Recent Additions
*   **PR #427:** Documented planar degeneracy of the three-gluon vertex. This is strictly **FRG INPUT** (boundary conditions), not the FRG solution itself.
*   **Phase 1-3 Sprint:** Established rigorous `mpmath` boundaries for the $\delta\gamma_{\text{bare}}$ calculation and surveyed lattice legacy data limits.
